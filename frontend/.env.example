# AgroVision AI Frontend Environment Variables
# Copy this file to .env and adjust as needed.

# Base URL used by the frontend for API calls in production builds.
# For local development with the Vite dev server, the built-in proxy handles it,
# so this can stay as the backend origin.
VITE_API_URL=http://localhost:8080
